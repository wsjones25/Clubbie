<h2>Clubs</h2>

<% @clubs.each do |club| %>
	<h4><%= club.club_name %></h4>
	<p><%= club.sport %></p>
	<p><%= club.address %></p>
	<p><%= link_to "More info", club_path(club) %></p>
	<%= image_tag club.main_image unless club.main_image.nil? %>
	<br>
<% end %>